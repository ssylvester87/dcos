#!/bin/bash -xe

mkdir -p "/build"

pushd "/pkg/src/xmlsec1"
./configure "--prefix=$PKG_PATH" --with-libxml=/opt/mesosphere --with-libxslt=/opt/mesosphere
make
make install

find "$PKG_PATH/lib/" ! -type d -name "*.a"  -delete
rm -rf "$PKG_PATH/share/"
